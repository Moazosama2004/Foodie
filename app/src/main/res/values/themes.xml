<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.Foodie" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/primary</item>
    </style>

    <style name="Theme.Foodie.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/primary</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/splash_logo</item>
        <item name="windowSplashScreenAnimationDuration">1000</item>
        <item name="postSplashScreenTheme">@style/Theme.Foodie</item>
    </style>

    <style name="Theme.Foodie" parent="Base.Theme.Foodie" />
    <style name="CircularImageView">
        <item name="cornerSize">50%</item>
    </style>
</resources>